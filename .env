VITE_ENVIRONMENT=development

# Committed default — must stay pointed at the hosted API, because the deploy
# builds from this file. For local development create `.env.local` (gitignored
# via *.local) with VITE_API_URL=http://localhost:4001; it takes precedence.
VITE_API_URL=https://backend.webschema.online
